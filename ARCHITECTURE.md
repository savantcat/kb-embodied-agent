# 技术架构（三层覆盖）

## 0. 一句话架构

```
用户开口 → ASR → 【我方 Agent 编排】 → RAG（企业知识库）+ LLM + MCP 工具 → 文本 + Widget 指令
                                                     ↓
                                       星云 SDK：TTS/SSML + 口型/表情/关键动作 + 字幕 + Widget 渲染
```

## 1. 分层与组件

```
┌─────────────────────────── 浏览器 / 大屏（web/） ───────────────────────────┐
│  XmovAvatar (WebGL, GPU 渲染)   │ 状态指示(Listen/Think/Speak) │ Widget 卡片  │
└───────────────▲────────────────────────────────────────────▲───────────────┘
                │ 会话凭证 / 音频 / 状态事件                   │ 文本+Widget 指令
┌───────────────┴────────────────────────────────────────────┴───────────────┐
│                        backend/  Agent 编排（FastAPI）                       │
│  /api/session  签发短时限域凭证（appSecret 不下发到前端）                      │
│  /api/chat     主链路：改写→检索→决策→生成→动作/Widget 指令                   │
│  /api/tools    MCP 工具清单与调用（行动层）                                   │
│  ├─ RAG 检索（企业语料，向量+关键词混合；无命中→明确"不知道"并转人工）           │
│  ├─ LLM（OpenAI 兼容：DeepSeek / 可替换）                                     │
│  └─ MCP Client（自研 MCP Server：查条款 / 出自查清单 / 算报价 / 转人工）        │
└────────────────────────────────────────────────────────────────────────────┘
```

## 2. 关键设计决策

| # | 决策 | 理由 |
|---|---|---|
| D1 | **关掉 SDK 内置 LLM 直连**（`auto_send_asr_to_llm=false`），由我方 Agent 接管 | SDK 不含 Function Calling / MCP，行动层必须自研；同时保证企业语料不出我方边界 |
| D2 | **前端不持有 appSecret**，由 `/api/session` 服务端签发 | 公开仓库安全要求（赛题明确指出 appSecret 暴露风险）；评审可复现且不泄密 |
| D3 | 断网/接口异常**自动降级到离线动画**并提示 | SDK 原生支持离线模式（不消耗积分）；对应评审"容错与降级"得分点，演示视频中显式展示 |
| D4 | 回答前**强制检索**企业语料，命中不足即"不知道 + 转人工" | 企业场景最怕编造；也是"为什么需要企业专属知识库"的价值支点 |
| D5 | 工具调用结果用 **Widget 卡片**呈现（非纯文本） | 对应命题"多模态 Widget 实时呈现执行结果"，与纯对话方案拉开差距 |
| D6 | 每个业务场景 = 一份 `scenario.yaml`（角色/语料/工具/话术） | 同一内核可切换门店导购、政务咨询、售后答疑，便于赛后复用与二次开发 |

## 3. 数据流（一次完整问答）

1. 用户语音 → SDK ASR → 文本事件
2. 上报 `/api/chat`；后端置状态 `Think`（前端显示"思考中"）
3. 意图判定：闲聊 / 知识问答 / 办事请求
4. 知识问答 → RAG 检索企业语料 → 组装带引用片段的提示词 → LLM 生成
5. 办事请求 → 选择 MCP 工具 → 调用 → 得到结构化结果 → 生成 Widget 指令
6. 返回 `{text, widgets, actions}`；前端 `speak()` 播报（SSML 分段）+ 渲染卡片 + 播放关键动作
7. 用户在播报中**打断** → SDK 中断 → 状态回到 `Listen` → 进入下一轮

## 4. 与赛题评分项的对应

| 评分项（权重） | 落点 |
|---|---|
| 场景创意与价值 35% | `docs/不可替代性论证.md`：门店/展厅"不会打字、看不清字、要边讲边指"三类真实约束 + 商业化路径 |
| 产品完成度与可用性 25% | Docker Compose 一键起、离线降级演示开关、断线重连、字幕与状态可视化、弱网演示 |
| 智能交互深度 25% | 多轮上下文 + 企业语料 RAG + **聆听/思考/说话/打断**与对话结合 + **行动层 MCP** |
| 开源规范 15% | Apache-2.0、README 四段式、依赖清单、CI、Issue/PR 模板、提交记录 |
